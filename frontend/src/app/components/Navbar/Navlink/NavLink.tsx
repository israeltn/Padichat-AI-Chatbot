type NavListItem = {
    title: string,
    path: string,
}
type NavList = [
   NavListItem
]

const NavLinks = => () {
    const navList = {}
    return (
        <div></div>
    )
}
export default NavLinks