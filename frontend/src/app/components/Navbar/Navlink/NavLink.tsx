type NavListItem = {
    title: string,
    path: string,
}
type NavList = [
   NavListItem
]

const NavLinks = => () {
    const navList = {}
    return (
        <div>giuy</div>
    )
}
export default NavLinks